{
  "private": true,
  "description": "POC for MDL Component architecture",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "rm -fr build/*",
    "build": "mkdir -p build && webpack",
    "build:min": "mkdir -p build && webpack -p",
    "dist": "npm run clean && npm run build && npm run build:min"
  },
  "devDependencies": {
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "css-loader": "^0.23.1",
    "eslint": "^2.12.0",
    "eslint-config-google": "^0.5.0",
    "extract-text-webpack-plugin": "^1.0.1",
    "lerna": "2.0.0-beta.18",
    "node-sass": "^3.7.0",
    "raw-loader": "^0.5.1",
    "sass-loader": "^3.2.0",
    "style-loader": "^0.13.1",
    "webpack": "^1.13.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
